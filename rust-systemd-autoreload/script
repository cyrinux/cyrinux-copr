#!/bin/sh -x
git config --global user.name "John Doe"
git config --global user.email johndoe@example.com

git clone https://git.sr.ht/~whynothugo/systemd-autoreload

rust2rpm -V -s ./${COPR_PACKAGE#rust-}
